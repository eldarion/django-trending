.. _installation:

Installation
============

* To install ::

    pip install django-trending


* Add ``trending`` to your ``INSTALLED_APPS`` setting::

    INSTALLED_APPS = (
        # other apps
        "trending",
    )
